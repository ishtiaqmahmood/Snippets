#include <iostream>
#include <filesystem>
#include <chrono>

namespace fs = std::filesystem;

int main() {
    // fs::path p = "test.txt";
    // auto last = fs::last_write_time(p);
    std::cout << "File watcher demo" << std::endl;
    return 0;
}
